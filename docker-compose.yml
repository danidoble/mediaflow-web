services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PUBLIC_APP_PORT:-4321}:80"
    environment:
      - PUBLIC_API_BASE_URL=${PUBLIC_API_BASE_URL:-http://localhost:8000}
      - PUBLIC_APP_NAME=${PUBLIC_APP_NAME:-MediaFlow}
    restart: unless-stopped
